#pragma once

#include "MathCommon.h"

namespace Math
{
	MATH_USAGE unsigned long Saturate(unsigned long value);
	MATH_USAGE float Saturate(float value);
	MATH_USAGE double Saturate(double value);
};
